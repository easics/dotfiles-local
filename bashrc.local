
smiley() {
  retval=$?
  if [ "$retval" = "0" ]; then
    echo ":)"
  else
    echo ":( ($retval)"
  fi
}

shopt -s direxpand

cdtools() {
  cd ~/src/git/easics/tools/$1
}

cdproject() {
  cd ~/src/git/$1/$2
}

cgr() {
  cd $(git root)
}

